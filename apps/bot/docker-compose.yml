services:
  zia:
    build: .
    container_name: zia-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - API_KEY=${API_KEY:-}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./settings.json:/app/settings.json
      - ./.env:/app/.env:ro
    ports:
      - "10000:10000"
